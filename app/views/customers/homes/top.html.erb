<div class="container">
  <div class="row">
    <div class="col-lg-2">
      <table class="table">
        <thead>
          <tr>
            <th>ジャンル検索</th>
          </tr>
        </thead>

        <tbody>
          <% @categories.each do |category| %>
            <tr>
              <td><%= link_to category.name, search_products_path(category), method: :get %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="col-lg-10">
      <div class="main-visual">
        <div class="main-visual-content">
          <p class="main-visual-text">
          ようこそ、NaganoCakeへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
          </p>
        </div>
      </div>
    <br>
      <h2>
        <strong>オススメ商品</strong>
      </h2>
      <% @products.each do |product| %>
        <div class="box" style="float:left; margin-right:0px;">
          <%= link_to product_path(product.id) do %>
            <%= attachment_image_tag product, :image, size: "160x110", format: 'jpeg', fallback: 'no_image.png' %>
          <% end %>
　　　　   <p><strong><%= product.name %></strong></p>
　　　　   <p><strong>¥<%= product.price %></strong></p>
        </div>
      <% end %>
      <%= paginate @products %>
      <%= link_to ' もっと見る ', products_path, class: '' %>
    </div>
  </div>
</div>